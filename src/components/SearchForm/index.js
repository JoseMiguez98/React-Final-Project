import React from 'react';
import './SearchForm.css';

const SearchForm = props => (
    <form id="search-form">
        <input type="text" placeholder="Type the name of your favorite artist..." />
    </form>
)

export default SearchForm;